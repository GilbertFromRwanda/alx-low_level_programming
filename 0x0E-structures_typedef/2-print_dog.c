#include "main.h"
#include "dog.h"
/**
 *print_dog - print
 *struct dog - dog struct
 *@d: for dog oject
 *
 */

void print_dog(struct dog *d)
{
if(d == NULL)
{
return ;
}
else
{
if(d->name == NULL)
{}
if(d->owner == NULL)
{
printf("")
}
if(d->age == NULL)
{

}
}
}
