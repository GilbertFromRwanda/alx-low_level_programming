#include<ctype.h>
#include "main.h"

/**
* _isdigit - user function
*@c: integer value
* Return: 0
*/
int main(void)
{
char c;
c = '0';
printf("%c: %d\n", c, _isdigit(c));
c = 'a';
printf("%c: %d\n", c, _isdigit(c));
return (0);
}
int _isdigit(int c)
{
return (isdigit(c) > 0 ? 1 : 0);
}
