#include "main.h"

/**
* puts - function code
*@str: string
* Return: 0
*/
int main(void)
{
    char *str;

    str = "0123456789";
    puts2(str);
    return (0);
}

void puts2(char *str)
{
int g, i = strlen(str);
for (g = 0; g < i; g = g + 2)
{
putchar(str[g]);
}
putchar('\n');
}
