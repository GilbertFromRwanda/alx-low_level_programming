#include "main.h"

/**
* _puts - function code
*@s: given string
* Return: 0
*/
int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}

void _puts(char *str)
{
puts(str);
_putchar('\n');
}
